<h2>Select to assign member</h2>
<br>
<%= form_for @assignment, :url => {:action => 'assigningmember'} do |f| %>
<table class="table">
   <tr>
    <th>select</th>
    <th>code</th>
    <th>name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @members.each do | member | %>
    <tr>
    <td><%= check_box_tag 'members[]', member.id %></td>
    <td><%= member.code %></td>
    <td><%= member.name %></td>    
    </tr>
  <% end %>
  </table>

<br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
