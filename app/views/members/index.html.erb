<h1>Listing members</h1>

<table class="table">
  <tr>
    <th>社員コード</th>
    <th>姓名</th>
    <th>Ｅメール</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @members.each do |member| %>
  <tr>
    <td><%= member.code %></td>
    <td><%= member.name %></td>
    <td><%= member.email %></td>
    <td><%= link_to 'Show', member %></td>
    <td><%= link_to 'Edit', edit_member_path(member) %></td>
    <td><%= link_to 'Destroy', member, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
  <tr>
  <td colspan="3"></td>
  <td colspan="2"><%= link_to 'New Member', new_member_path %></td>
  <td colspan="1"><%= link_to 'CSV Download', members_path(format:"csv") %></td>
  </tr> 
</table>
<%= will_paginate @members, :previous_label=>'≪前', :next_label=>'次≫' %>
<br />
<br />
<%= form_tag 'members/upload', :multipart => true do %>
  メンバー一括登録:（*.csv）
  <%= file_field_tag 'upload_file', :accept => 'text/comma-separated-values' %>
  <%= submit_tag "アップロード", :confirm => "アップロードしていいですか？" %>
<% end %>
<br />
