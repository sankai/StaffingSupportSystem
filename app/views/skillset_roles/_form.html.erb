<%= form_for(@skillset_role) do |f| %>
  <% if @skillset_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skillset_role.errors.count, "error") %> prohibited this skillset_role from being saved:</h2>

      <ul>
      <% @skillset_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :memo %><br />
    <%= f.text_field :memo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
